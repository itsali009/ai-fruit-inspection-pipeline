# -*- coding: utf-8 -*-
"""Task3_MobileNetV3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1XPdZHV3Am-ONpbs9nIms9DPeoTkaVyG8

Preliminary Experiments:Quality Control:MobileNetV3

## Step 1: Importing Libraries
start by importing the necessary libraries for data preprocessing, model building, training, and evaluation.
"""

# Import necessary libraries
import tensorflow as tf
from tensorflow.keras.preprocessing.image import ImageDataGenerator
from tensorflow.keras.applications import MobileNetV3Large
from tensorflow.keras.layers import Dense, GlobalAveragePooling2D
from tensorflow.keras.models import Model
from sklearn.metrics import classification_report, confusion_matrix
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

"""## Step 2: Load and Preprocess the Data
Use ImageDataGenerator for data augmentation to improve generalization during model training and prepare the validation dataset.
"""

# Define paths for training and validation datasets
train_dir = '/content/drive/MyDrive/Fruits-Quality/Training'
valid_dir = '/content/drive/MyDrive/Fruits-Quality/Validation'

# Define image size and batch size
IMG_SIZE = (224, 224)
BATCH_SIZE = 32

# Data augmentation for training data
train_datagen = ImageDataGenerator(
    rescale=1./255,
    rotation_range=20,
    width_shift_range=0.2,
    height_shift_range=0.2,
    zoom_range=0.2,
    horizontal_flip=True,
    fill_mode='nearest'
)

# Rescale validation data
valid_datagen = ImageDataGenerator(rescale=1./255)

# Load training and validation data
train_generator = train_datagen.flow_from_directory(
    train_dir,
    target_size=IMG_SIZE,
    batch_size=BATCH_SIZE,
    class_mode='categorical'
)

valid_generator = valid_datagen.flow_from_directory(
    valid_dir,
    target_size=IMG_SIZE,
    batch_size=BATCH_SIZE,
    class_mode='categorical'
)

"""## Step 3: Build the MobileNetV3 Model
Now load MobileNetV3Large, pretrained on ImageNet, and fine-tune it for the quality control classification task.
"""

# Load the MobileNetV3Large model with pre-trained ImageNet weights
base_model = MobileNetV3Large(weights='imagenet', include_top=False, input_shape=(224, 224, 3))

# Freeze the base layers to avoid training them
base_model.trainable = False

# Add custom layers on top of MobileNetV3Large
x = base_model.output
x = GlobalAveragePooling2D()(x)  # Global pooling to flatten the output
x = Dense(512, activation='relu')(x)  # Fully connected layer
predictions = Dense(train_generator.num_classes, activation='softmax')(x)  # Output layer

# Create the full model
model = Model(inputs=base_model.input, outputs=predictions)

# Compile the model
model.compile(optimizer='adam', loss='categorical_crossentropy', metrics=['accuracy'])

# Model summary
model.summary()

"""## Step 4: Train the MobileNetV3 Model
Train the model using the training dataset and validate it using the validation dataset.
"""

# Train the MobileNetV3 model
EPOCHS = 10

history = model.fit(
    train_generator,
    steps_per_epoch=train_generator.samples // BATCH_SIZE,
    validation_data=valid_generator,
    validation_steps=valid_generator.samples // BATCH_SIZE,
    epochs=EPOCHS
)

"""## Step 5: Evaluate the MobileNetV3 Model
After training, evaluate the model using metrics like accuracy, precision, recall, F1-score, and confusion matrix.
"""

# Get predictions and true labels
validation_steps = valid_generator.samples // BATCH_SIZE + 1
Y_pred = model.predict(valid_generator, validation_steps=validation_steps)
y_pred = np.argmax(Y_pred, axis=1)
y_true = valid_generator.classes

# Generate classification report
print("Classification Report:")
print(classification_report(y_true, y_pred, target_names=valid_generator.class_indices.keys()))

# Confusion matrix
conf_matrix = confusion_matrix(y_true, y_pred)

# Plot confusion matrix
plt.figure(figsize=(10, 8))
sns.heatmap(conf_matrix, annot=True, fmt='d', cmap='Blues', xticklabels=valid_generator.class_indices.keys(), yticklabels=valid_generator.class_indices.keys())
plt.xlabel('Predicted Label')
plt.ylabel('True Label')
plt.title('Confusion Matrix')
plt.show()

"""## Step 6: Plot Accuracy and Loss Curves
Plot the training and validation accuracy and loss over the epochs to monitor the model's performance.
"""

# Plot accuracy and loss over epochs
acc = history.history['accuracy']
val_acc = history.history['val_accuracy']
loss = history.history['loss']
val_loss = history.history['val_loss']

epochs_range = range(EPOCHS)

plt.figure(figsize=(12, 6))

# Plot accuracy
plt.subplot(1, 2, 1)
plt.plot(epochs_range, acc, label='Training Accuracy')
plt.plot(epochs_range, val_acc, label='Validation Accuracy')
plt.legend(loc='lower right')
plt.title('Training and Validation Accuracy')

# Plot loss
plt.subplot(1, 2, 2)
plt.plot(epochs_range, loss, label='Training Loss')
plt.plot(epochs_range, val_loss, label='Validation Loss')
plt.legend(loc='upper right')
plt.title('Training and Validation Loss')

plt.show()

"""## Step 7: Save the Model
Save the trained MobileNetV3 model for future use
"""

# Save the trained MobileNetV3 model
model.save('mobilenetv3_quality_control_classifier.h5')